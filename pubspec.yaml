name: smart_kitchen_assistant
description: AI Smart Kitchen Assistant for reducing household food waste.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  cloud_firestore: ^6.0.3
  firebase_auth: ^6.1.1
  firebase_core: ^4.2.0
  flutter_local_notifications: ^21.0.0
  flutter_timezone: ^5.0.2
  intl: ^0.19.0
  shared_preferences: ^2.5.5
  timezone: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
